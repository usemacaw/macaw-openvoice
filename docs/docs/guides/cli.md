---
title: CLI
---

Macaw ships with a CLI for serving the runtime and working with audio files.

```bash
macaw serve
macaw transcribe audio.wav --model faster-whisper-large-v3
macaw translate audio.wav
macaw list
macaw inspect faster-whisper-large-v3
```

Use `Macaw --help` for the full command reference.
