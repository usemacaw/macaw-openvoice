services:
  macaw:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - macaw-models:/root/.macaw/models
    environment:
      - MACAW_LOG_FORMAT=json
    restart: unless-stopped

volumes:
  macaw-models:
